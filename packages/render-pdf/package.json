{
  "name": "@markforge/render-pdf",
  "version": "0.1.0",
  "private": true,
  "description": "IR to PDF via Typst (ADR-0003)",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "dependencies": {
    "@markforge/ir": "workspace:*"
  },
  "markforge": {
    "browserTier": "deferred"
  },
  "devDependencies": {
    "@myriaddreamin/typst-ts-node-compiler": "0.7.0"
  },
  "devDependenciesComment": "Tests compile their generated Typst rather than asserting its shape: the #figure( defect was a compile failure that produced perfectly plausible-looking source, so a string assertion would have passed through it (brief 13)."
}
