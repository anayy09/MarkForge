# Gates — what is checked, and where each was seen to fail

**Generated by `scripts/check-gates.mjs`. Do not edit by hand.**
Run `node scripts/check-gates.mjs --update` to regenerate; CI fails when the committed
copy has drifted.

This document exists because the repository's entire discipline rests on gates and there
was no list of them. The Phase 6 audit that produced it found **ten gates with no negative
control at all** — each reporting success in a way an empty input would also report — and
**two divergences** between `pnpm verify` and `ci.yml`, on a tree where `pnpm verify` was
green.

## What the columns mean

**Seen to fail** carries two forms, and the distinction matters more than the entries.

- **control** — the gate runs a negative control on *every invocation*, so its ability to
  fail is re-proved each run rather than recorded once. Stronger, and every gate has it.
  It proves that a specific violation produces a failure. It does not prove the gate is
  right: two controls in this repository were vacuous, and no mechanism here would have
  caught either.
- **a commit** — the gate was observed failing on real input. Weaker, because a gate can
  rot after the commit that proved it, but it is evidence about the repository rather than
  about the gate's own arithmetic.

`pnpm verify` and `ci.yml` must run the same set; `scripts/check-gate-parity.mjs` asserts
it, and the two exemptions there are for capability (network, a pinned pandoc) rather than
convenience.

## The gates

| Gate | Asserts | Seen to fail | `verify` | `ci.yml` |
| --- | --- | --- | --- | --- |
| `check-docs.mjs` | Every Phase 0–5 deliverable named in a doc exists, cross-references resolve, and nothing under fixtures/local/ is tracked | **control**, every run | ✔ | ✔ |
| `check-schemas.mjs` | The three JSON Schemas compile under ajv strict, the worked IR examples validate, and a target profile without verifiedAgainst is rejected | **control**, every run | ✔ | ✔ |
| `check-fixtures.mjs` | Every committed fixture carries a licence line, every licence line names a file that exists (CORPUS §1 rule 1), and CORPUS.md's 15 categories agree with STATUS.md's states and with the fixtures on disk | **control**, every run — an unregistered fixture was reported and exited 1; the category arm's first run caught STATUS.md calling §2.5 done when it is HTML-only | ✔ | ✔ |
| `build-messy-fixtures.mjs` | The nine committed §2.3 messy DOCX fixtures still match their generator, byte for byte | **control**, every run | ✔ | ✔ |
| `build-corpus-fixtures.mjs` | The eight committed CORPUS §2.2, §2.5, and §2.12 fixtures still match their generator, byte for byte | **control**, every run — authoring them found the DOCX adapter never reads footnotes.xml, and the generator's own tc() helper escaped a nested table into a text run — caught by censusing the parsed IR, not by reading the generator | ✔ | ✔ |
| `build-agentify-corpus.mjs` | The agentify source sets match their generator, the near-duplicate pairs stay below Jaccard 0.2, and the hard negatives are hard | **control**, every run | ✔ | ✔ |
| `build-scanned-fixtures.mjs` | The committed scan still rasterises to the same bytes, so the LLM cache keyed on its page-image digest still hits | **control**, every run | ✔ | ✔ |
| `build-reference-templates.mjs` | The three templates match their generator, define all 38 Pandoc style names, carry TEMPLATES §2.1's constructs, and contain zero direct formatting | **control**, every run | ✔ | ✔ |
| `check-markdown-lint.mjs` | Every rendered Markdown file satisfies the markdownlint rule set with no autofix pass (ADR-0006) | **control**, every run | ✔ | ✔ |
| `diff-mammoth.mjs` | Every divergence between our OOXML reader and Mammoth is triaged in docs/MAMMOTH-DIFF.md (ADR-0005) | **control**, every run | ✔ | ✔ |
| `check-agentify.mjs` | The §10 measurements: traceability at 1.0, the one-region diff, dedup recall and precision, conflicts, budget, and the stale-verification warning | **control**, every run + `ec58f47` — the traceability gate's heading allowlist accepted an invented heading under 40 characters | ✔ | ✔ |
| `check-browser-bundle.mjs` | No package in ADR-0015's eager set reaches a node: builtin, a Node global, or a dynamic require | **control**, every run + `033de5a` — its first run failed all ten packages ADR-0015 named | ✔ | ✔ |
| `check-http-retention.mjs` | The HTTP API retains no document: filesystem delta, retrieval routes, minted ids, and cross-request contamination, each against a retaining control | **control**, every run + `80f4aeb` — all four probes catch a deliberately retaining control server | ✔ | ✔ |
| `check-surface-parity.mjs` | CLI, HTTP, MCP, and browser produce identical bytes for 30 conversions, and --strict exits 2 on a partial LLM failure | **control**, every run + `70c8744` — the --llm-against-a-dead-endpoint probe exited 0 with ok:true | ✔ | ✔ |
| `check-degradation.mjs` | Every catch block declares what it does with the failure, and every emits MF-XXX-0000 annotation names a code the diagnostics table defines | **control**, every run + `dadcb35` — the annotation search found its own annotation, and a wrong MF-PDF-0004 sat under it | ✔ | ✔ |
| `check-merge-predicate.mjs` | CORPUS §2.14.1's merge predicate agrees with every corpus answer key, and a known-true restatement is not called different facts | **control**, every run + `dadcb35` — the first implementation called a known-true restatement different facts | ✔ | ✔ |
| `check-fixture-contamination.mjs` | No graded fixture appears in the prompt that grades it: verbatim containment, a shared six-content-word run, and the signature predicate | **control**, every run + `dadcb35` — §2.16's three graded cases were all worked examples inside the adjudicator prompt | ✔ | ✔ |
| `check-test-collection.mjs` | Every test file is collected and every skip is declared, so a suite cannot silently shrink on a clean clone | **control**, every run + `dadcb35` — 611 tests locally against 606 on a clean clone, 5 of them unannounced | ✔ | ✔ |
| `check-adr-enforcement.mjs` | Every ADR names the check that enforces it, and that check exists and runs | **control**, every run + `b211b7e` — ADR-0012 named a check that had nothing to do with it | ✔ | ✔ |
| `check-status-claims.mjs` | Every STATUS.md state cell is checked or explicitly marked unverified, names an artifact that exists and runs, and reports no untraceable figure | **control**, every run + `b211b7e` — a row naming scripts/check-total-fiction.mjs passed | ✔ | ✔ |
| `check-target-docs.mjs` | docs/TARGETS.md is derived from targets/*.json, so a note about data cannot contradict the data | **control**, every run | ✔ | ✔ |
| `check-gate-parity.mjs` | The set of gate scripts in `pnpm verify` equals the set in ci.yml's blocking jobs, modulo a reasoned exemption table | **control**, every run — its first run found build-scanned-fixtures.mjs and run-fidelity.mjs gating the branch and absent from the local chain, then caught itself as verify-only | ✔ | ✔ |
| `run-fidelity.mjs` | No fidelity metric drops more than 0.005 below its committed baseline (SPEC §9.6, exit 4) | **control**, every run — the control's own first version perturbed a field no entry carries and reported 0 regressions where it expected 1 | ✔ | ✔ |
| `run-scoreboard.mjs` | MarkForge does not fall below Pandoc or word-to-markdown-js on any of the 28 metric-fixture pairs it currently ties or wins | **control**, every run — the control's metric floor was set to 5 and failed against the intended 4 | — | ✔ |
| `check-node-type-coverage.mjs` | Every node type ir.v0.schema.json declares is produced by an adapter and consumed by a renderer, or is an enumerated exception or a ROADMAP-recorded gap | **control**, every run — its first run reported 13 unproduced types; a probe showed 8 were the detector being wrong rather than the code, and the 4 that survived — equationBlock, comment, citation, textBox — were all invisible to the census | ✔ | ✔ |
| `check-ir-structure.mjs` | Parsed IR matches a hand-written declaration, and SPEC §9.2's block separator holds across every multi-block container in the corpus | **control**, every run — built after tables-block-content scored 100% on every round-trip metric while a cell's three paragraphs read as one run-together word — a defect applied to both sides of a round trip agrees with itself | ✔ | ✔ |
| `check-flavor-distinctness.mjs` | SPEC §4.1's seven flavour presets produce seven byte-distinct renders of one construct-dense document, and CommonMark reports the footnote it cannot express (ADR-0021) | **control**, every run — its first runs found strong:'**' illegal, a block math node degraded to an inline node collapsing a whole document onto one line, a capability check reached by every label in a shared case group, and three presets tying because the adapter never produced an admonition at all | ✔ | ✔ |
| `check-pdf-determinism.mjs` | PDF output is byte-identical across separate processes, carries no wall-clock date, and reports every construct Typst cannot express (ADR-0003) | **control**, every run — its first run found the renderer walked an unmapped node's empty children array before reporting the loss, so a textBox slipped through silently; before that, two compiles in separate processes differed at byte 11533 in /CreationDate | ✔ | ✔ |
| `check-pdf-fonts.mjs` | Every font embedded in a rendered PDF is one fonts/ ships, so output cannot depend on the host machine's installed faces (SPEC §4.3), and every declared exception is proved to need a font we do not ship | **control**, every run — its first run found unicode-edge-cases, cjk-chinese and cjk-japanese embedding SimSun, ArialMT, YuGothic and SegoeUIEmoji from the developer's Windows machine — invisible to the determinism gate, which compares two processes on one host — and then rejected rtl-hebrew.md as an unearned exemption, because Libertinus does cover Hebrew | ✔ | ✔ |
| `check-hook.mjs` | The shipped pre-commit hook rejects unformatted staged Markdown, accepts it once formatted, ignores non-Markdown, and reads the index rather than the working tree | **control**, every run — case 5 removes the hook and re-commits the same rejected content, so a rejection that came from somewhere else would be reported rather than counted as a pass | ✔ | ✔ |
| `check-producer-exports.mjs` | A DOCX produced by the real Pandoc binary parses into a schema-valid IR whose headings, lists, and tables survive (CORPUS §2.15) | **control**, every run — its first run found every Pandoc export invalid — TOCHeading declares w:outlineLvl 9 and the schema capped outlineLevel at 8, against ISO/IEC 29500-1's 0-9 range — and the control re-raises the value past the new cap so a schema that stopped checking would be reported | ✔ | ✔ |
| `check-template-strict.mjs` | All three shipped templates exit 2 under --strict for constructs Markdown cannot express, and a control document exits 0 | **control**, every run — it failed on main as inline CI shell: clean-report.docx exited 2 where the step expected 0, because the adapter had begun recovering the caption and description list it always contained. Its control asserts a document that must exit 0, so a build where every conversion exited 2 would fail rather than pass | ✔ | ✔ |
| `check-gates.mjs` | Every gate that runs has a row here, every row runs, every row can fail, and docs/GATES.md matches the ledger | **control**, every run — its first run reported itself as an undocumented gate | ✔ | ✔ |
| `fetch-ocr-assets.mjs` | Not a gate. Downloads tesseract language data and the found scan so the fidelity job measures the set the committed baselines describe | n/a — not a gate | — | ✔ |

33 gates, 10 with a recorded failing commit, all with a negative control that runs every invocation.
